# configs/config.yaml
gcp:
  project_id: "taxi-data-engineer"
  region: "us-central1"
  bucket: "taxi-data-engineer-taxi-lake"

databricks:
  catalog: "taxi_lakehouse"

bigquery:
  dataset: "taxi_analytics"

thresholds:
  quarantine_rate_max: 0.10   # design.md §11 fail-fast gate (covers ~8.9% observed TLC raw quarantine rate)

